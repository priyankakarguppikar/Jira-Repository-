pipeline {
     agent any
     stages {
         stage('Build') {
             steps {
                 echo 'Building...'
             }
             post {
                 always {
                     jiraComment body: 'Build ', issueKey: 'JP-*'
                 }
             }
         }
         }
     }
 
