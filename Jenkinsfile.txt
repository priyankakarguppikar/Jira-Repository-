pipeline {
     agent any
     stages {
         stage('Build') {
             steps {
                 echo 'Building...'
             }
             post {
                 always {
                     jiraComment body: 'Build for this project is created in the Jenkins', issueKey: 'JP'
                 }
             }
         }
         }
     }
 
