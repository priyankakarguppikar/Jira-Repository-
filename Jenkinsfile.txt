pipeline {
     agent any
     stages {
         stage('Build') {
             steps {
                 echo 'Building...'
             }
             post {
                 always {
                     jiraSearch 'project = JP'
                 }
             }
         }
         }
     }
 
